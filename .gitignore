# Chrome profiles — contain your logged-in Vinted session (cookies/tokens). NEVER commit.
vinted_profile/
vinted-profile/

# Python / tooling caches
__pycache__/
.ruff_cache/
*.pyc

# Logs & backups
*.log
*.bak
*-bak

# Internal planning — contains pricing & client-negotiation strategy. Keep private.
PROJECT_GOALS.md
HANDOFF.md

# Phase 5 — image-embedding cache (large, regenerable) & per-run cluster outputs
phase5_cache/
visual_variants_*.csv
